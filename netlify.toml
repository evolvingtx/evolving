# Evolving Therapy & Wellness — Netlify Configuration

[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"

[build.environment]
  ANTHROPIC_API_KEY = ""
  TRIAL_TOKEN_SECRET = ""
